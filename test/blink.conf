
#config.txt
# Basic Information:
filename = "Sample Configuration File"; #Comment
#
# GPIO
#
#This sets the mode of a pin to either INPUT, OUTPUT, or PWM_OUTPUT.
#For every pin defined here, you have to define also the mode of the pin.
#Note that only wiringPi pin 1 (BCM_GPIO 18) supports PWM output. The pin number is the number obtained from the pins table.
GPIO1 = 0;
GPIO1_M = "OUTPUT";

#
# Scribe
#
Scribe = {
host = "localhost";
port = 1463;
cat = "buckettest";
};

#
# MySQL
#
MySQL = {
host = "localhost";
user = "test";
pass = "test";
db = "pihome";
port = "3306";
};
